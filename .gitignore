# Build outputs
bin/
obj/
Releases/
publish/

# The prebuilt portable exe - built by CI / Build-NetMonGui.bat, not committed
/NetMonGui.exe

# KernelTraceControl.dll self-extracts here next to whatever exe is running
# (see ProcessBandwidthService.EnsureNativeDependencyExtracted)
/amd64/

# Velopack packaging tool cache
.vpk/

# IDE
.vs/
*.user

# Claude Code local settings (personal, not project config)
.claude/

# Stream Deck plugin - Node deps (its built bundle already matches bin/ above),
# plus the SDK's own runtime log files when run/tested locally
node_modules/
streamdeck-plugin/com.danielwinter.netmon.sdPlugin/logs/
