{
  "name": "netmon-streamdeck-plugin",
  "version": "1.0.0",
  "private": true,
  "description": "Stream Deck plugin showing live ping/jitter from NetMonGui on a key.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "dependencies": {
    "@elgato/streamdeck": "^2.1.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "esbuild": "^0.28.2",
    "typescript": "^5.7.3"
  }
}
